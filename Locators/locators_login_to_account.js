export const login_to_account_locators = {
    login_form_locator : '[data-testid = login-Button]',
    email_locator : '[data-testid = user-Email-input]',
    password_locator : '[data-testid = userPassword-input]',
    button_login_locator : '[data-testid = login-Form-Submit-Button]',
    check_account_locator : '[data-testid = header-account-link]',
};